- [x] Resolve the merge conflict in `docs/principles/open-source.md` and finalize the intended content.
- [x] Add missing `docs/about/authors.md` and `docs/about/contact.md`, or remove/update their references in `mkdocs.yml`.
- [x] Add missing `docs/projects/census/about.md` and `docs/projects/census/deprivare.md`, or remove/update their references in `mkdocs.yml`.
- [x] Add missing `docs/developer/local-dev/pyenv.md`, or remove/update its reference in `mkdocs.yml`.
- [x] Add missing `docs/_utilities/page-template.md`, or update `docs/developer/writing-documentation.md` to remove/replace the reference.
- [x] Add missing `docs/developer/api-python.md` (and its anchors), or update `docs/developer/writing-documentation.md` to remove/replace the reference.
- [x] Wire `includes/_abbreviations.md` into the MkDocs pipeline (e.g., `pymdownx.snippets` `auto_append`) or remove the claim in that file.
- [x] Decide on plugin usage: configure `mkdocs-git-revision-date-localized-plugin` and `mkdocs-macros-plugin` in `mkdocs.yml` or remove them from `requirements.txt`.
- [x] Align `mkdocs.yml` `site_url` with the intended playbook domain referenced in `README.md`.
- [x] Replace placeholder text in `docs/developer/writing-documentation.md` (e.g., `INSERT LINK`, `ADD LINK`) with real URLs or remove.
- [x] Add a `.python-version` file or update `docs/developer/writing-documentation.md` to avoid referencing it.
- [x] Fix the Dockerfile header comment to match the actual base image version (Python 3.12).
- [x] Reconcile the “Material for MkDocs Insiders” claim in `docs/developer/writing-documentation.md` with the actual dependency (`mkdocs-material`) or switch to the insiders package.
- [x] Review `mkdocs.yml` `edit_uri` (points to `live`) vs contribution guidance (PRs to `development`) to reduce contributor confusion.
- [x] Decide whether `docs/developer/api-docker.md` should be linked from navigation or removed to avoid drift.
- [x] Update the Material for MkDocs base to use the new Zensical framework, to avoid the upstream problems mentioned here https://squidfunk.github.io/mkdocs-material/blog/2026/02/18/mkdocs-2.0/
- [ ] deprecate the abbreviations file in favour of having links which go to a suitable informational web link. This is more informative and less likely to be out of date. If we do this, we should remove the claim in `includes/_abbreviations.md` that it is auto-appended to all pages.
